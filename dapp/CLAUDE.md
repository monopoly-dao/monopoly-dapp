# Repository Guidelines

## Branch and PR Workflow

- **Base branch**: All PRs should be opened to merge into `dev-v2` branch
- **Feature branches**: Create new branches off `dev-v2`
- This ensures consistent workflow and prevents accidental merges to `main`

## Development Environment

- Running `npm run dev` starts the dev server
- The project uses Next.js 14.1.0 with TypeScript